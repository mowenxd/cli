# mocli - 关于墨问 API Key

API Key 是调用墨问 API 的私密凭证，墨问不会明文保存任何用户的私密凭证，所以获取了之后用户需要自行保存好。

## 如何获取

`墨问`小程序 ->  右下角`我的` -> 找到`开发者`选项 -> `我的 API Key`

## 遗失与更换

一旦遗失，墨问无法再次提供之前的 API Key，只能再次重新生成。一旦重新生成新的 API Key，旧有的 API Key 即时失效。